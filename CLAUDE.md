@AGENTS.md

# Claude Code-specific instructions

Treat the imported `AGENTS.md` as canonical. Keep shared project settings in
`.claude/settings.json`, private overrides in `.claude/settings.local.json`, and
team MCP definitions in `.mcp.json`. Do not add executable hooks without a
documented threat review and a disabled-by-default example.
